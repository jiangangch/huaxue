<view class="container">
  <view class="page-title">上海初中化学</view>
  <view class="page-subtitle">章节知识点总览（内容后续完善）</view>

  <block wx:for="{{chapters}}" wx:key="title">
    <view class="card">
      <view class="card-title">{{item.title}}</view>
      <view class="card-desc">{{item.summary}}</view>
    </view>
  </block>
</view>